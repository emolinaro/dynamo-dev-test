# Official commands from the Dynamo docs / upstream launch scripts.
# These are not yet adapted into Kubernetes manifests in this repo.

# vLLM-Omni text-to-image
bash examples/backends/vllm/launch/agg_omni_image.sh

curl -s http://localhost:8000/v1/images/generations \
  -H "Content-Type: application/json" \
  -d '{
    "model": "Qwen/Qwen-Image",
    "prompt": "A cat sitting on a windowsill",
    "size": "1024x1024",
    "response_format": "url"
  }'

# SGLang image diffusion
cd $DYNAMO_HOME/examples/backends/sglang
./launch/image_diffusion.sh

curl -s http://localhost:8000/v1/images/generations \
  -H "Content-Type: application/json" \
  -d '{
    "model": "black-forest-labs/FLUX.1-dev",
    "prompt": "A sunset over the ocean",
    "size": "1024x1024",
    "response_format": "url",
    "nvext": {
      "num_inference_steps": 15
    }
  }'
